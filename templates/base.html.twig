<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        <!-- Inclure jQuery -->
        <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
        <!-- Inclure Bootstrap CSS et JavaScript -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

        {% block stylesheets %}
             {% endblock %}
        {% block javascripts %}
             {% endblock %}
    </head>
    <header>
        <header class="bg-light text-black">
            <div class="container d-flex justify-content-between align-items-center py-2">
                <div class="search-section">
                    <form class="search-form form-inline" action="{{ path('searchMedia') }}" method="get">
                        <select class=" search-select " name="media">
                            <option value="movie">Films</option>
                            <option value="tv">Séries</option>
                        </select>
                        <input class="form-control me-2 search-input" type="search" name="query" placeholder="Recherche" aria-label="Recherche">
                        <button class="btn btn-outline-dark search-button" type="submit">Rechercher</button>
                    </form>
                </div>
                <ul class="nav">
                    <li class="nav-item m-1">
                        <a class="nav-link btn btn-outline-dark" href="{{ path('popularFilm')}}">
                            <span class="span-nav-custom">Film</span>
                        </a>
                    </li>
                    <li class="nav-item m-1">
                        <a class="nav-link btn btn-outline-dark" href="{{ path('popularSerie')}}">
                            <span class="span-nav-custom" >Serie</span>
                        </a>
                    </li>
                    <li class="nav-item m-1">
                        <a class="nav-link btn btn-outline-dark" href="{{ path('all')}}">
                            <span class="span-nav-custom" >Favoris</span>
                        </a>
                    </li>
                    {% if app.user %}
                        {# If connected #}
                        <li class="nav-item m-1">
                            <a class="nav-link btn btn-outline-dark" href="{{ path('app_logout')}}">
                                <span class="span-nav-custom" >Deconnexion</span>
                            </a>
                        </li>
                    {% else %}
                        {# If NOT connected #}
                        <li class="nav-item m-1">
                            <a class="nav-link btn btn-outline-dark" href="{{ path('app_login')}}">
                                <span class="span-nav-custom" >Se connecter</span>
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </header>
    </header>
    <body class="bg-dark">
        {% block body %}{% endblock %}
    </body>
</html>
